# Loaded by scripts/cargo-cranelift.sh (nightly + -Zcodegen-backend only).
# Dev crate uses Cranelift; dependencies stay on LLVM for compile time and compatibility.

[unstable]
codegen-backend = true

[profile.dev]
codegen-backend = "cranelift"

[profile.dev.package."*"]
codegen-backend = "llvm"
