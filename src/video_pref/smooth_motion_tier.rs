/// **`mpv`** **`vf vapoursynth:`** frame-queue depth (fixed for bundled FlowFPS). Bundled ME px² is **`RHINO_SMOOTH_MAX_AREA`** (`publish_smooth_me_budget_env`).
pub(crate) const SMOOTH_VF_BUFFERED_FRAMES: i32 = 4;
